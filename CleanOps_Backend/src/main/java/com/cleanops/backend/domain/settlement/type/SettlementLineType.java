package com.cleanops.backend.domain.settlement.type;

public enum SettlementLineType {
    REVENUE,
    REFUND,
    COMPENSATION,
    OUTSOURCING_COST,
    REWORK_COST,
    REDISPATCH_COST,
    MANUAL_ADJUSTMENT
}
