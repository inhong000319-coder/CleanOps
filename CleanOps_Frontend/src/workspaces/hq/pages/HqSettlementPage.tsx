/**
 * HQ 정산 관리 페이지 — 플레이스홀더
 * 기준: settlement.md (CONFIRMED 상태는 수정 버튼 비노출, adjustment-only)
 *
 * 2차 작업에서 구현:
 * - 정산 회차 목록 (SettlementStatus 배지)
 * - CONFIRMED 상태에서는 수정 버튼 비노출, adjustment 버튼만 표시
 * - SoD: 초안 작성자 ≠ 확정자 (확정 버튼은 draft 작성자에게 비노출)
 */

export function HqSettlementPage() {
  return (
    <div>
      <h2>정산 관리</h2>
      <p style={{ color: '#999', fontSize: 14 }}>2차 작업에서 구현 예정</p>
    </div>
  )
}
